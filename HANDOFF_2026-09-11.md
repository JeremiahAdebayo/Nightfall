# Nightfall — Handoff (2026-09-11)

**Authoritative project-state summary. Supersedes HANDOFF_2026-09-10.md
except its §10 notebook provenance map, which is still the reference.**

---

## 1. TL;DR

The 2026-09-10 restructure is committed AND pushed (`b06b03c "Project
restructuring"`, verified on the remote via the GitHub API — note the
canonical repo casing is now `JeremiahAdebayo/Nightfall`; the old URL
redirects). Today's session closed the top three items of the old §9
priority list plus §7.5, and every commit below is pushed to `main`.

| | |
|---|---|
| HEAD | `8e86c66` + the CI/weights_only commit described in §2.4 |
| Tests | **13 passing** (was 9) |
| Hard blocker (unchanged) | No real model inference here — `download.pytorch.org` DNS-blocked; Colab re-verification of §3 items 1–4 of the 09-10 doc is still pending |
| Do NOT | `git clean`, `git checkout -- .`, `git reset --hard` |

## 2. What changed today (all pushed)

### 2.1 `1f0232d` — trailing newline on `nightfall/core/feature_extractor.py`
The one working-tree edit left over from the restructure; brings the file
to the state §6 of the 09-10 doc described.

### 2.2 `da4c321` — `--refit-bank` on `scripts/run_eval_int8.py` (old §9.1, the highest-integrity gap)
- Default mode unchanged (mismatched-bank baseline, honestly documented).
- `--refit-bank`: refits each category's bank from
  `<data-root>/<cat>/train/good` with the SAME INT8 extractor used for
  scoring — the DECISION.md train/test feature-space invariant, and the
  notebook cells-23/24 finding (hazelnut 0.486→1.000) made runnable.
- `--save-refit`: writes refit banks to `<cat>_memory_bank_int8.pt`
  (never overwrites fp32 checkpoints); later `--refit-bank` runs reload
  them automatically. Path helper `int8_checkpoint_path()` added to
  `nightfall/config.py` (+ convention test).
- `weights_only=True` on the checkpoints this script loads.
- README + `docs/colab.md` document both modes.

### 2.3 `8e86c66` — notebook cell 19 promoted (old §9.2)
- `nightfall/eval/quantization_geometry.py`: pure metric core — top-1
  nearest-neighbour preservation, top-k overlap, Spearman of top-k
  values, margin statistics — with the "why this matters" story in the
  docstring (margins explain the reweighting collapse).
- `scripts/quantization_geometry.py`: CLI; `--bank-source fp32|int8`
  mirrors run_eval_int8's two modes; `--bank-source int8` refuses to run
  without a saved refit bank.
- 3 new tests on synthetic distance matrices; lazy export added to
  `nightfall/eval/__init__.py`; README tree updated.

### 2.4 This commit — CI + `torch.load` hardening (old §9.3, §7.5/§9.6)
- `.github/workflows/ci.yml`: `pip install -e ".[dev]"` + `pytest` on
  push/PR (Python 3.11, ubuntu). No dataset/GPU needed.
- `weights_only=True` added to the four remaining bare `torch.load`
  call sites: `nightfall/serving/run_grpc_server.py`,
  `scripts/{calibrate_thresholds,run_eval,train}.py`. Audit now clean
  (the only multi-line `torch.load(` without the flag on its first line
  is train.py's wrapped call, which has it on the continuation).

## 3. Verification (executed here)

`py_compile` on every touched file; `pytest` → 13 passed;
`run_eval_int8.py --help` and `quantization_geometry.py --help` (flags
and two-mode docs render); `quantize_onnx.py --help` (carried over).
Still NOT verified here: any real inference — the Colab re-run list in
§8 of the 09-10 doc stands, now plus:
`run_eval_int8.py --refit-bank --save-refit` then
`quantization_geometry.py --bank-source int8` for the consistent-pipeline
numbers nobody has recorded yet.

## 4. Remaining work (re-prioritised)

1. **Colab re-verification** — the only place with GPU + data + weights;
   closes every "NOT verified" item. Command chain in §3 above.
2. §9.5 of the 09-10 doc: programmatic mask/image pairing assertion in
   `nightfall/eval/dataloader.py` (its docstring still describes the
   silent-mismatch risk).
3. §7.7: project-local venv (still installing into the noctis-swebench
   venv).
4. §7.3: `fp32_reference` dict in `run_eval_int8.py` duplicates the
   README table — single-source-of-truth question, still open.
5. §7.1: regenerate `nightfall_pb2_grpc.py` package-safe so the
   hand-edited try/except sibling import stops being a footgun.
6. §7.6: no CI existed — DONE today; watch the first Actions run for
   pip/torch install time (CUDA wheels on ubuntu; consider the CPU index
   if it's too slow).

## 5. Environment notes

- `git add/commit/push` to `.git` requires the escalated sandbox here;
  normal-shell git commands fail with `.git/index.lock: Permission
  denied` (and network git fails with an schannel credentials error).
  Escalated git worked fine all session.
- `.pytest_cache/` is unreadable/unwritable in this sandbox — pytest
  emits a cache warning every run; harmless.
- `AGENTS.md` in this root got committed in `b06b03c` (it mentions
  noctis-swebench; see §7.8 of the 09-10 doc — still worth resolving).
- `HANDOFF_2026-09-10.md` is now committed too, with a superseded
  banner; §10 of it (notebook provenance map) remains the reference.
